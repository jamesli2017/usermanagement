//println "Project name is $name"
//apply plugin:'java'

publishing {
  publications {
    login(MavenPublication) {
	  groupId 'io.homenet'
	  artifactId 'login'
	  version '1.0'
	  
	  from components.java
	}
  }
}

task loginTask {
  doLast {
    println "Task name is $name"
  }
}